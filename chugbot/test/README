To run the tests, run ./runTests.sh from this directory.

PHPUnit may complain that your version of PHP is too old: if this happens, simply upgrade your
PHP version, and try again (make sure the upgraded PHP is first in your PATH).
